# 批量图片处理功能使用说明

## 📋 功能概述

系统已升级为支持**最多10张图片同时上传和批量修复**的功能。

## ✨ 主要改进

### 1. 多文件上传
- ✅ 支持一次性选择最多10张图片
- ✅ 支持拖拽上传（将图片拖到上传区域）
- ✅ 实时显示已上传图片数量（X/10）

### 2. 顺序遮罩绘制
- ✅ 点击缩略图进入编辑模式
- ✅ 为每张图片单独绘制遮罩
- ✅ 完成的图片会显示 ✓ 标记
- ✅ 可随时返回修改任意图片的遮罩
- ✅ **新增手型工具**：支持缩放和平移图片，方便精确绘制细节

### 3. 批量处理
- ✅ 一键处理所有图片
- ✅ 实时进度显示
- ✅ 单张图片失败不影响其他图片
- ✅ 显示处理统计信息（成功/失败数量、总耗时）

### 4. 结果展示
- ✅ 网格布局显示所有修复结果
- ✅ **三个导航选项卡**：可在"修复后"、"SAIN 结构感知叶脉图"和"修复前"之间切换
- ✅ 每张结果卡片包含：
  - 原始文件名
  - 对应的图片（修复后/叶脉图/原始图）
  - 处理耗时（仅修复后）
  - 单独下载按钮（修复后和叶脉图）

### 5. 下载功能
- ✅ 单张图片下载
- ✅ 全部结果打包为ZIP下载
  - 包含修复图片和边缘图
  - 自动按原文件名命名

### 6. 管理功能
- ✅ **智能清除**：仅在修复完成后，再次上传时自动清除旧数据
- ✅ **累积上传**：在上传/编辑阶段，可以多次点击上传图片，累积添加（最多10张）
- ✅ 无需手动清除，流程更流畅

## 🚀 使用流程

### 步骤1：上传图片
1. 点击"上传图片 (最多10张)"按钮
2. 或者直接将图片拖拽到上传区域
3. 选择1-10张图片（支持PNG、JPG格式）

### 步骤2：绘制遮罩
1. 在缩略图网格中点击第一张图片
2. 在编辑器中用红色画笔涂抹病斑/破损区域
3. 点击"保存遮罩并返回"
4. 重复步骤1-3，直到所有图片都完成遮罩绘制
5. 完成的图片会显示绿色 ✓ 标记

### 步骤3：批量修复
1. 当所有图片都绘制完遮罩后，"一键智能修复"按钮会自动激活
2. 点击"一键智能修复"
3. 等待处理完成（进度条显示处理状态）

### 步骤4：查看和下载结果
1. 修复完成后，结果会以网格形式展示
2. **使用导航选项卡对比不同视图**：
   - 点击"修复后"按钮：查看所有修复后的图片
   - 点击"SAIN 结构感知叶脉图"按钮：查看所有叶脉边缘图
   - 点击"修复前"按钮：查看所有原始上传的图片
3. 可以点击查看每张图片
4. 点击"下载此结果"下载单张修复图片（仅在"修复后"标签页）
5. 点击"下载叶脉图"下载单张叶脉图（仅在"SAIN 结构感知叶脉图"标签页）
6. 点击"下载全部结果 (ZIP)"打包下载所有结果

## ⚠️ 注意事项

1. **图片数量限制**：最多只能上传10张图片
2. **遮罩必须完成**：所有图片都必须绘制遮罩后才能开始修复
3. **处理时间**：10张图片大约需要8-10秒（取决于硬件）
4. **内存占用**：批量处理会占用较多GPU显存
5. **文件格式**：仅支持PNG和JPG格式

## 🔧 技术实现

### 后端新增接口
- `/api/repair_batch` - 批量处理接口
- `/api/download_batch` - ZIP打包下载接口
- `process_single_image()` - 单张图片处理函数（重构）

### 前端新增组件
- 拖拽上传区域
- 缩略图网格
- 进度条
- 结果网格
- ZIP下载功能

## 📊 性能指标

- 单张图片处理时间：~81ms
- 10张图片总处理时间：~810ms +  overhead
- 支持并发处理：顺序处理（避免显存溢出）
- 最大批量大小：10张（可配置）

## 🐛 常见问题

**Q: 为什么我的"一键智能修复"按钮是灰色的？**
A: 请确保所有上传的图片都已绘制遮罩（显示✓标记）。

**Q: 如何处理超过10张图片？**
A: 请先处理前10张，完成后点击"全部清除"，再上传下一批。

**Q: ZIP下载的文件在哪里？**
A: 浏览器会下载到默认下载文件夹，文件名为 `repaired_images.zip`。

**Q: 某张图片处理失败会影响其他图片吗？**
A: 不会，系统会继续处理其他图片，并在结果中标记失败的图片。

## 📝 更新日志

**v2.4 (2026-04-07)**
- ✨ 新增"手型"工具（✋）
- ✨ 支持鼠标滚轮缩放图片（0.5x-5x）
- ✨ 支持鼠标拖动平移图片
- ✨ 方便精确绘制小病斑和细节

**v2.3 (2026-04-07)**
- ✨ 新增"SAIN 结构感知叶脉图"导航选项卡
- ✨ 支持查看和下载叶脉边缘图
- ✨ 三个选项卡：修复后 | SAIN 结构感知叶脉图 | 修复前

**v2.2 (2026-04-07)**
- ✨ 删除“全部清除”按钮
- ✨ 优化上传逻辑：仅在修复完成后再次上传时才清除旧数据
- ✨ 支持在上传阶段累积添加图片（多次点击上传）
- ✨ 提升用户体验，流程更流畅

**v2.1 (2026-04-07)**
- ✨ 新增"修复前/修复后"导航选项卡
- ✨ 支持一键对比修复前后的图片
- ✨ 优化结果展示界面

**v2.0 (2026-04-07)**
- ✨ 新增批量图片处理功能（最多10张）
- ✨ 新增拖拽上传支持
- ✨ 新增缩略图网格视图
- ✨ 新增顺序遮罩绘制工作流
- ✨ 新增进度条显示
- ✨ 新增结果网格展示
- ✨ 新增ZIP打包下载
- ✨ 新增"全部清除"功能
- 🔧 重构后端API，提取通用处理函数
- 🎨 优化UI/UX设计

---

如有问题或建议，请联系开发团队。
